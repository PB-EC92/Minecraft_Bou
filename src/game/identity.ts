/**
 * Nom et version du jeu, en un seul endroit (J7). Le nom est provisoire : les
 * enfants choisiront le nom définitif (brief) ; le changer ici suffit (titre de
 * l'onglet, messages, panneau Tests). Le format des sauvegardes garde, lui, son
 * identifiant technique « cubes » (clés `cubes:`, champ app des exports).
 *
 * Version : « 1.0-rc » (candidate) jusqu'à la recette complète avec les enfants,
 * puis « 1.0 ».
 */
export const GAME_NAME = "Cubes";
export const VERSION = "1.0-rc";
